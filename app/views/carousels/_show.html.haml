.row
  .col-md-4.mx-auto.text-center
    %h5= link_to @carousel.user.full_name, artist_profile_path(@carousel.user, artist_profile_section: 'Work')
    %h6= @carousel.name

-# %i.fa.fa-chevron-right
-# %i.fa.fa-camera-retro

.row
  .col-md-10.mx-auto
    .card.border-0.box-shadow.p-4
      = react_component("Slider", { images: carousel_image_hashes(@carousel) })
