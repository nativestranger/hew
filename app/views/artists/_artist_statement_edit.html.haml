.form-inputs
  = simple_form_for @user, url: update_profile_settings_path do |f|
    = f.error_notification
    = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

    .row
      .col-12
        .form-group
          = f.input :artist_statement, input_html: { class: 'form-control' }, label: false, as: :trix_editor

    = f.button :submit, 'Save My Artist Statement', class: 'btn btn-primary'
